export interface Category {
    id: string;
    name: string;
    description: string;
    isHighSchool: boolean;
    isElementary: boolean;
    };
  
//  what I need for this-  category name, category description, id, boolean -> high or middleSchool since a project can only be highSchool
//  or Only for a middleSchool or Both. NO timestampp.